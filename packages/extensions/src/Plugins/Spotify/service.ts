const base_url = "https://www.spotifycharts.com"

function getTopCharts(){
    // in for india and global returns general top 200
    const region = 'global'
    const url = `${base_url}/regional/${region}/daily/latest/`
}
